# 云集代理 · 朋友端「内核无法启动 + 状态栏重叠」诊断与修复

> 诊断来源：`[15:54] 朋友的APP诊断` 日志（设备 `com.yunjii.proxy`，mihomo 内核）
> 结论：**核心崩溃 = 配置 YAML 顶层键重复导致 mihomo 解析失败**；状态栏重叠 = WebView 未预留安全区。

---

## 一、现象（来自诊断日志）

- `进程存活: 否` / `代理运行: 否` / `端口响应: 否` / `看门狗: 停止`
- 致命错误（贯穿全文）：
  ```
  hub.Parse() failed: yaml: unmarshal errors:
    line 63: mapping key "secret" already defined at line 3
    line 64: mapping key "mixed-port" already defined at line 4
    line 65: mapping key "allow-lan" already defined at line 5
    line 66: mapping key "log-level" already defined at line 6
    line 67: mapping key "proxies" already defined at line 7
    line 74: mapping key "proxy-groups" already defined at line 14
  ```
- 连通性：`直连网络: 是`、`TUN连通: 是`，但 `代理连通: 否`、`外部控制器 127.0.0.1:9090 不可达`（因为内核根本没起来）。
- 状态栏：App 顶栏与手机系统状态栏（时间/电量）重叠。

## 二、根因

**1. 配置被错误拼接成两份（致命）**

朋友设备上的 `config.yaml`（`/data/user/0/com.yunjii.proxy/files/mihomo/config.yaml`，约 208 行）
里，`secret / mixed-port / allow-lan / log-level / proxies / proxy-groups` 这些**顶层键各出现了两次**——
前半段是第一份（proxy-groups 8 个），后半段又重复了一次（proxy-groups 10 个，并带 rules/dns/tun 等）。

mihomo 的 YAML 解析（`hub.Parse`）**不允许重复顶层键**，于是在 `Mobile.start()` 阶段直接抛错，
内核从未启动 → 翻不了墙、外部控制器端口起不来。

**为什么你手机正常、朋友的不正常？**
App 会把远程线路配置（gitlab 上的 Alvin9999 `config.yaml`）下载缓存到
`line_<线路名>.yaml`。若当时远程文件本身就是「两份拼在一起」的异常版本（或缓存到了异常副本），
朋友端就会用到坏的 `line_*.yaml`；而你的手机此前缓存的是干净副本，所以正常。
App 现有的清洗逻辑（`injectTunIntoConfig` / `ensure*`）对**标量键用 `replaceAll`（会保留全部重复项）**，
对 `proxies` / `proxy-groups` **完全不动**——所以重复项被原样写进了 `config.yaml`。

**2. 状态栏重叠（外观）**

原生布局是 `CoordinatorLayout` 内嵌 `WebView`，主题里 `android:statusBarColor=transparent`、
`fitsSystemWindows=false`（即边缘到边缘沉浸式）。但前端 `index.html` 的 viewport **没有 `viewport-fit=cover`**，
且 `.top-bar` 没有为状态栏预留 `env(safe-area-inset-top)` 内边距 —— 于是 WebView 内容顶到 y=0，
顶栏图标/标题压在系统状态栏上。

## 三、已做的修复（源码层面）

### 修复 1：配置去重自愈（核心）
文件：`web/frontend/android/app/src/main/java/com/yunjii/proxy/MihomoPlugin.java`
- 新增 `dedupeTopLevelKeys(String)`：按顶层键拆分，**保留每个键的「最后一次出现」**，把重复拼接的
  配置收敛成一份完整合法的配置（重复份里更完整的那份被保留）。
- 在 `injectTunIntoConfig()` 与 `ensureChinaDnsConfig()` 末尾调用。
  所有写 `config.yaml` 的路径（启动 / 切线路 / 还原 / 保存配置）都会经过这两个方法之一，因此**全部覆盖**。
- 效果：朋友端的坏 `line_*.yaml` 不需要手动清理，App 下次启动时自动生成合法 `config.yaml`，内核即可启动。

### 修复 2：状态栏安全区
- `web/frontend/index.html`：viewport 增加 `viewport-fit=cover`（开启 `env(safe-area-inset-*)` 支持）。
- `web/frontend/src/components/TopBar.vue`：`.top-bar` 增加 `padding-top: env(safe-area-inset-top, 0px)`，
  让深色顶栏填充状态栏区域、标题/图标下移到系统状态栏之下。
- `web/frontend/src/styles/main.css`：`.van-tabbar` 增加 `padding-bottom: env(safe-area-inset-bottom, 0px)`（底部手势条安全区，顺带修复）。

## 四、给朋友的「立即解药」（无需改代码）

1. **重新构建并发送修复后的 APK** → 朋友安装/覆盖后，App 在下次启动会自动重写合法 `config.yaml`，内核自愈。
   （覆盖安装不会清除旧的 `line_*.yaml`，但去重逻辑会在生成 `config.yaml` 时兜底修复，无需清缓存。）
2. 若想更快验证：让朋友在 App 内点「重新获取线路 / 更新配置」（重新下载 `line_*.yaml`），
   或「清除应用数据」后重开，触发干净重建。
3. 状态栏修复需随新 APK 一起生效（属于前端资源）。

## 五、验证

用 Python 移植 `dedupeTopLevelKeys` 跑了一份「与诊断一致」的坏配置（上述 6 个键各重复一次）做验证：
- 修复前：21 个顶层键，6 个重复 → 与诊断报错完全吻合；
- 修复后：**0 个重复**，且更完整的 10 项 `proxy-groups` 被保留，`rules/dns/sniffer/tun/profile` 齐全。

> 已生成的去重样例：`C:\Users\Administrator\AppData\Local\Temp\config_fixed.yaml`（按诊断中的订阅重建，
> 仅作算法验证，非朋友真机文件；真机文件由 App 下次启动自动生成）。
